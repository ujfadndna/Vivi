# Docker mock/runtime dependencies. Keep this close to requirements.txt but use
# headless OpenCV so the default image stays smaller and avoids GUI libraries.
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.1
pydantic-settings==2.3.0
cryptography==42.0.8

celery==5.4.0
redis==5.0.4

opencv-python-headless==4.9.0.80
numpy==1.26.4
imageio-ffmpeg==0.5.1
soundfile==0.12.1

pytest==8.2.0
httpx==0.27.0
